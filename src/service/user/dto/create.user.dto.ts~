import { Prisma } from '@prisma/client';

export class CreateUserDto implements Prisma.UserCreateInput {
    telegramId: string;
    username?: string;
    firstName?: string;
    lastName?: string;
    
}